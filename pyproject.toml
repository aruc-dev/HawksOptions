[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F"]
ignore = [
  # CLI entrypoints add the repo root to sys.path before importing local modules.
  "E402",
  # Existing code is not consistently wrapped at Ruff's default line length.
  "E501",
]
